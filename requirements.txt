# Core Data Science Libraries
pandas>=1.5.0
numpy>=1.23.0
scipy>=1.9.0

# Machine Learning
scikit-learn>=1.2.0
xgboost>=1.7.0
imbalanced-learn>=0.10.0  # For SMOTE

# MLflow Experiment Tracking
# Note: CML tracking server is compatible with MLflow 2.x
# MLflow 3.x has breaking changes (run_uuid -> run_id)
mlflow>=2.8.0,<3.0.0

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# CML/Cloudera Specific (if not pre-installed)
# cml>=1.0.0  # Usually pre-installed in CML workspaces

# Spark (if needed, usually pre-installed in CML)
# pyspark>=3.3.0

# Utilities
tqdm>=4.64.0  # Progress bars (optional)

# Development/Testing (optional)
# pytest>=7.2.0
# black>=22.10.0
# flake8>=5.0.0
